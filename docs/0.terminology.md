---
git_hash: "19d3873474ff7e6bad2a48fe0466fc26d281fc52"
modified: "2026-08-15"
---

# Terminology

This glossary defines the vocabulary used across the project — the docs, [`scripts/build.mjs`](scripts/build.mjs), the POI catalogue and the hosted tile services. It expands the original five-entry skeleton (Zoom, Liberty, OpenMapTiles, POI, OpenStreetMap) into a complete reference: [Core Concepts](#core-concepts), [Dependencies](#dependencies), [Acronyms](#acronyms), [Naming conventions](#naming-conventions), and the things we weren't sure about in [Notes & open questions](#notes--open-questions).

> [!NOTE]
> **Zoom shorthand.** Zoom levels are written as `z` + number, e.g. z12. See [Zoom](#core-concepts).

## Core Concepts

**Zoom** — The current zoom of the map (usually 0–18 or 0–20). Referred to using the shorthand `z[zoom_number]`, e.g. z12. Features declare zoom ranges via `minzoom`/`maxzoom` (MapLibre's `maxzoom` is exclusive); _overzoom_ happens when the renderer draws tiles beyond their declared maxzoom.

**Anchor** — The layer id relative to which a build section splices its layers, preserving the surrounding stack order. The POI stack's anchor starts at Liberty's `poi_r20` and moves to the first new POI tier layer (`outdoor-poi-z1`) once the Liberty POIs are replaced.

**Allowlist** — The class/kind lists derived from the POI catalogue and baked into layer filters (e.g. `tier1Classes`, `tier2Classes`). A class on the allowlist renders at its tier zoom regardless of OMT rank.

**Base style** — The upstream OSM Liberty style (OpenFreeMap fork) that the build deep-clones and mutates in place. See [Liberty](#dependencies).

**Brunnel** — _Bridge/tunnel_ — the combined bridge/tunnel attribute in the OMT `transportation` layer, used by the road filters to keep bridges and tunnels out of the surface-aware hierarchy.

**Cadence** — The elevation interval of minor contour lines (`CONTOUR_MINOR_EVERY`, 20 m). The cadence must divide the 100 m index interval evenly so minor lines land symmetric between index pairs.

**Casing** — The contrasting outline drawn under a road or route fill. Among the route tiers only the regional (RWN) tier gets a casing.

**Catalogue** — `pois/catalogue.yml`, the single source of truth for the POI system: every POI the style shows, its data source, zoom, icon and label behaviour. Entries are either `ofm` (an OMT class allowlist, with optional `rank_max` density cap) or `custom` (an OSM tag selector for the hosted extract, with `include_when`, `min_zoom` and `show_title`). See [Outdoor POIs](4.pois.md).

**Class** — A value-space term used three ways: OMT feature class (`rock`, `farmland`, `military`, `swimming_pool`…), OMT POI class (`campsite`, `park`, `beer`, `ferry_terminal`…), and mountain-peak class (`saddle`, `volcano`). The POI tiers are class allowlists.

**Compare app** — The Vue dev app (`dev/`) that renders the outdoor style side-by-side with external providers (OpenFreeMap, MapTiler, Thunderforest, Waymarked Trails, Esri, USGS…) via `@maplibre/maplibre-gl-compare`. It also converts contour labels to feet at runtime (`applyImperialContours()`). A static build (`npm run demo:build`) produces the deployable `demo/` output.

**Data floor** — The zoom below which a source carries no data. The OMT `poi` layer's data floor is z14; tiers that want POIs below that (huts from z8, most kinds z10–13) need the custom hosted extract.

**Decimation** — Hiding off-cadence contour lines by painting them at opacity/width 0 rather than filtering them out, because MapLibre GL v5 filter syntax cannot express modulo.

**Density strategy** — The route-gated path density used by the paths profile: below z12, paths are emitted only when part of a walking-route relation (iwn → z9, nwn → z10, rwn → z11, lwn → z12); all paths from z12.

**Extract** — A region of OSM data. Either a Geofabrik extract used as Planetiler input (`geofabrik:italy` for local test builds) or the "custom extract" — the hosted planet build that supplies POIs, paths and routes below the OMT data floor.

**Feature toggle** — A boolean constant at the top of [`scripts/build.mjs`](scripts/build.mjs) that gates a style section; flip it and rebuild. See [The Style Build](1.build.md).

**Filter** — A style-layer expression that decides which features render. The build targets legacy filter syntax (`$type`, no "Multi-" prefixes) because of MapLibre GL v5 behaviour — see [The Style Build](1.build.md).

**Glyphs** — The font glyph PBFs (`{fontstack}/{range}.pbf`) used for symbol labels; provided by Orange Mug in the base style.

**Handoff** — The zoom transition between the low-zoom paths overlay (z9–13) and the base `road_path_pedestrian` layer at z14, styled as one visual family. (The word is also used for a road-casing zoom-transition flash — see [Notes & open questions](#notes--open-questions).)

**Hillshade** — The 2D relief-shading layer (`hillshade-layer`) derived from the DEM, fading in across the low zooms.

**Index contour** — The bold contour line at every 100 m (`ele % 100 === 0`), which also carries the elevation label. Contrast _minor contour_.

**Kind** — The custom-POI category field (`hut`, `shelter`, `water`, `viewpoint`, `pass`, `trailhead`…), distinct from the OMT _class_. `ELE_KINDS` are the kinds that also carry an `ele` elevation attribute.

**Layer** — A single styled visual element in the style (id, type, source, paint/layout properties). Build sections add/replace/remove/restyle layers; new layers use kebab-case ids, often `outdoor-` prefixed — see [Naming conventions](#naming-conventions).

**Minor contour** — The thin contour lines on the 20 m cadence between index lines, decimated at higher zooms.

**Overlay** — A hosted vector source layered on top of the base style: the outdoor POIs, paths, routes and contours served from `tile.ogis.app`. The Planetiler profiles use an `isOverlay()` flag to mark themselves as overlays. See [Tile Server](7.server.md).

**Overzoom** — Rendering tiles beyond their declared maxzoom (the renderer scales up the last available zoom level).

**POI** — _Point of Interest_. The project's POI system is catalogue-driven: `ofm` entries render OMT POI classes at fixed tiers; `custom` entries render hosted-extract kinds; both map to icons in the Maki sprite. See [Outdoor POIs](4.pois.md).

**Promotion / promoted** — Giving track and service roads earlier visibility (from z12), thicker widths and name labels from z13, promoting them beyond base data.

**Rank** — OMT's per-cell POI importance number, used with `rank_max` density caps so dense classes (bus stops, post boxes, city parks) never flood a tile; also the mountain-peak rank (1 vs 2–3) and the park-label rank (`PARK_LABEL_MAX_RANK`).

**Raster-dem** — The MapLibre source type for elevation tiles; the project's instance is the Mapterhorn `demSource`.

**Route network tiers** — The OSM walking-route network values, longest-distance first: IWN (international), NWN (national), RWN (regional), LWN (local). Each tier is styled with its own colour and zoom. See the [Acronyms](#acronyms) table for expansions.

**Section** — A build-time style transformation (`apply…()` function) in [`scripts/build.mjs`](scripts/build.mjs), ordered bottom→top of the render stack and gated by a feature toggle.

**Source** — A data provider in the style: `openmaptiles` (vector base), `ne2_shaded` (raster relief), `demSource` (raster-dem), and the hosted overlay sources `contour-source`, `outdoor-paths`, `outdoor-poi`, `outdoor-route`.

**Source-layer** — The named sub-layer inside a vector source that layers reference (e.g. `outdoor_pois`, `contours`, `mountain_peak`); snake_case.

**Sprite** — The icon sprite sheet (Maki, served by OpenFreeMap as `ofm_f384/ofm`). `icon-image` values are validated against the sprite at build, failing the build on a missing icon rather than rendering nothing.

**Style** — The MapLibre style: `style.json` at the project root, generated by the build from the Liberty base and validated against the MapLibre style spec. It is the only artefact the style needs to ship.

**Surface-aware** — The road hierarchy keyed on paved/unpaved `surface`, with unpaved roads drawn narrower and dashed.

**Terrain** — The 3D elevation rendering enabled by the style's `terrain` property (`{ source: "demSource", exaggeration: 1.5 }`).

**Tier** — A zoom-ordered rendering band. Used for the POI zoom tiers (tier 1 → `outdoor-poi-z1`, z12+; tier 2 → `outdoor-poi-z2`, z14+), the route network tiers, and the peak label tiers.

**Tile** — A square (usually 256 px) map tile at zoom z, column x, row y: `{z}/{x}/{y}.pbf` (vector), `.webp`/`.png` (raster).

**Toggle** — see [Feature toggle](#core-concepts).

## Dependencies

**Liberty (OSM Liberty)** — The base style this project starts from: [maputnik/osm-liberty](https://github.com/maputnik/osm-liberty), tracked via the [OpenFreeMap fork](https://github.com/hyperknot/openfreemap-styles) `main` branch (cached locally with ETag invalidation). Out of the box it provides an excellent world map: OpenMapTiles vector data, Natural Earth relief, Maki icons and Orange Mug glyphs. The build keeps Liberty's labels, water, boundaries and rail, and replaces the visual layers (landcover, landuse, roads, buildings, POIs) with outdoor-first versions; its POI layers (`poi_r1/r7/r20`) give way to the catalogue-driven tiers.

**OpenMapTiles (OMT)** — The vector-tile schema for the base map ([openmaptiles.org](https://openmaptiles.org/)), translating OSM tags into layers such as `poi`, `transportation`, `mountain_peak` and `landcover`. Its zoom-gating shapes the project's data floors: no path geometry below z12, and the `poi` layer emits nothing below z14 — the reason the custom overlays exist.

**OpenStreetMap (OSM)** — The source data for everything: the planet dataset of nodes, ways and relations described by `key=value` tags (`tourism=alpine_hut`, `highway=path`, `route=hiking`), delivered as planet PBFs or Geofabrik extracts. Attribution: `© OpenStreetMap contributors`.

**OpenFreeMap (OFM)** — The free planet-wide vector tile provider ([openfreemap.org](https://openfreemap.org/)) behind the base style: `tiles.openfreemap.org` (constant `OFM_DOMAIN`), the `ofm` POI catalogue entries, and the sprite (`ofm_f384/ofm`). Also the source of the Liberty fork and of the compare app's Bright/Dark/Fiord/Liberty/Positron styles.

**Mapterhorn** — The DEM provider ([mapterhorn.com](https://mapterhorn.com/)): Terrarium-encoded WebP raster tiles at `tiles.mapterhorn.com` (512 px, maxzoom 17, Copernicus GLO-30 base + national/regional LiDAR). Powers the hillshade, the 3D terrain and — server-side — the contours.

**Planetiler** — The tile generator ([onthegomap/planetiler](https://github.com/onthegomap/planetiler)) used to build the hosted overlays from OSM data: declarative YAML schemas (the POIs) and imperative Java profiles (paths, routes), outputting a pmtiles archive. Java profiles require JDK 21+.

**MapLibre** — The open-source map renderer the style targets: MapLibre GL JS / Native, the [style specification](https://maplibre.org/maplibre-style-spec/) the build validates against, and the compare widget. npm dependencies: `maplibre-gl`, `@maplibre/maplibre-gl-style-spec`, `@maplibre/maplibre-gl-compare`.

**Maki** — The icon set ([mapbox/maki](https://github.com/mapbox/maki/)) used for POI icons. Liberty builds its sprite from an old snapshot of Maki; the catalogue maps class/kind → sprite glyph name explicitly, with a `marker` fallback and build-time sprite validation.

**contour-mvt-server** — The hosted contour service ([acalcutt/contour-mvt-server](https://github.com/acalcutt/contour-mvt-server)) at `tile.ogis.app/terrain`: generates PBF contour tiles on demand with marching squares over the Mapterhorn DEM. See [Tile Server](7.server.md).

**Geofabrik** — The regional OSM extract provider ([download.geofabrik.de](https://download.geofabrik.de/)) used as Planetiler input (`geofabrik:italy` for local builds, the full planet PBF for production).

**Natural Earth** — The pre-shaded relief raster inherited from the base style (`ne2_shaded` source, maxzoom 6); not DEM-derived. See [Style Structure](2.style.md).

**Orange Mug** — The font-glyph provider ([orangemug/font-glyphs](https://github.com/orangemug/font-glyphs)) used by the base style's labels.

**Open GIS (ogis.app)** — The hosting project behind `tile.ogis.app`, which serves the POIs, paths, routes and contours, and lends the style its working title ("Open GIS Outdoors Style").

**Compare-app providers** — The external styles the dev compare app renders against ([`dev/src/providers.json`](dev/src/providers.json)): OpenFreeMap (Bright, Dark, Fiord, Liberty, Positron); MapTiler (Outdoor v4, Satellite, Topo v2 — API key); Thunderforest (Atlas, Landscape, Outdoors — API key); Waymarked Trails (cycling, hiking, MTB, riding, skating, slopes); Esri (NatGeo, Shaded Relief, Terrain, World Imagery, World Topo); USGS (Imagery, Imagery Topo, Topo); plus CyclOSM, OpenCycleMap, OpenTopoMap, OpenSeaMap, OpenSnowMap Pistes, MtbMap and the OSM Mapnik raster.

## Acronyms

| Acronym               | Expansion                                                   | Meaning in this project                                                                                    |
| --------------------- | ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| CAI                   | Club Alpino Italiano                                        | Italian alpine club; the `cai_scale` route attribute (emitted by the routes profile, not yet styled)       |
| CDN                   | Content Delivery Network                                    | Serves the shared Mapterhorn DEM twice from cache (client fetch + contour service fetch)                   |
| CLI                   | Command-Line Interface                                      | The npm script entrypoints (`build`, `validate:style`, `check:pois`…)                                      |
| CORS                  | Cross-Origin Resource Sharing                               | The hosted tile services are open-CORS for client use                                                      |
| DEM                   | Digital Elevation Model                                     | The Mapterhorn elevation source, and the feature toggles gating it (`DEM`, `DEM_HILLSHADE`, `DEM_TERRAIN`) |
| ETag                  | Entity tag                                                  | HTTP cache-validation header used for Liberty/upstream cache invalidation                                  |
| GL                    | Graphics Library                                            | Part of the MapLibre GL (JS/Native) names                                                                  |
| HMR                   | Hot Module Replacement                                      | Vite reloads the compare app when `style.json` is rebuilt                                                  |
| HTTP                  | HyperText Transfer Protocol                                 | Web transfer protocol                                                                                      |
| IWN / NWN / RWN / LWN | International / National / Regional / Local Walking Network | OSM route network values; the route tier hierarchy                                                         |
| JSON                  | JavaScript Object Notation                                  | Style and configuration data format                                                                        |
| JS                    | JavaScript                                                  | The project's scripting language                                                                           |
| MTB                   | Mountain Bike                                               | The `mtb_scale` attribute, its styling section, and the Waymarked Trails MTB provider                      |
| MVT                   | Mapbox Vector Tiles                                         | The vector tile format (spelled out in full in the build script)                                           |
| NE2                   | Natural Earth 2                                             | The shaded-relief raster (`ne2_shaded` source; tile path segment `ne2sr`)                                  |
| OFM                   | OpenFreeMap                                                 | Tile provider and Liberty fork; the `ofm` catalogue entries                                                |
| OMT                   | OpenMapTiles                                                | The base vector schema                                                                                     |
| OSM                   | OpenStreetMap                                               | Source data; `© OpenStreetMap contributors` attribution                                                    |
| OSMC                  | OpenStreetMap Cartography                                   | The `osmc:symbol` route-marker attribute (emitted by the routes profile, not yet styled)                   |
| PBF                   | Protocol Buffer Binary                                      | Vector tile wire format (`{z}/{x}/{y}.pbf`); also `*.osm.pbf` planet extracts                              |
| PMTILES               | PMTiles                                                     | Single-file tile archive output by the Planetiler profiles                                                 |
| POI                   | Point of Interest                                           | The outdoor POI system (catalogue, tiers, overlays)                                                        |
| RAM                   | Random Access Memory                                        | Planetiler planet builds need substantial RAM and scratch disk                                             |
| RGB                   | Red, Green, Blue                                            | Colour channels used by Terrarium elevation encoding                                                       |
| SAC                   | Swiss Alpine Club                                           | The `sac_scale` hiking-difficulty attribute (emitted by the paths profile, not yet styled)                 |
| SQL                   | Structured Query Language                                   | OMT upstream `poi_class()` query referenced by the POI coverage check                                      |
| TBD                   | To Be Done                                                  | `// TBD: Slice N` stubs in the build script                                                                |
| URL                   | Uniform Resource Locator                                    | Tile and style URLs                                                                                        |
| XYZ                   | X/Y/Z                                                       | Tile grid shorthand `{z}/{x}/{y}`                                                                          |
| YAML                  | YAML Ain't Markup Language                                  | The catalogue and generated POI schema format                                                              |

## Naming conventions

- **Layer ids** — new project layers use kebab-case, usually `outdoor-` or feature-prefixed: `outdoor-poi(-z1/-z2)`, `outdoor-paths-*`, `outdoor-route-<network>`, `contour-lines`, `contour-labels`, `hillshade-layer`, `landcover-rock`… Restyled Liberty layers keep their original snake_case ids (`water`, `road_path_pedestrian`).
- **Source-layer names** — snake_case: `outdoor_pois`, `outdoor_paths`, `outdoor_routes`, `contours`, `mountain_peak`.
- **Zoom-tier suffix** — `-z1`/`-z2` marks the POI zoom tiers.
- **Constants** — `SCREAMING_SNAKE_CASE`; every colour literal lives in the `COLOURS` object (`COLOURS.<FEATURE>.<KEY>`). Referenced by name, never by line number.
- **Source ids** — `openmaptiles`, `ne2_shaded`, `demSource`, `contour-source`, `outdoor-paths`, `outdoor-poi`, `outdoor-route`.

## Notes & open questions

> [!NOTE]
> These are the things we were not fully sure about while writing this glossary. Corrections welcome.

1. **`ofm_f384`** — the sprite URL qualifier. The meaning of `f384` (sprite resolution factor? variant?) is not documented anywhere in the repo.
2. **`ne2sr`** — the tile path segment under `/natural_earth/ne2sr/` is inferred to be "Natural Earth 2 shaded relief", but the expansion is not spelled out anywhere.
3. **Unstyled attributes** — the Java profiles emit `sac_scale`, `cai_scale`, `osmc_symbol`, `operator`, `distance`, `ascent`, `descent` and `roundtrip`, but no style code reads them yet. Presumed carried for future use.
4. **Route tier zooms** — the data density gates (iwn z9 / nwn z10 / rwn z11 / lwn z12, in the Java profile and [Tile Server](7.server.md)) differ from the paint minzooms in `ROUTE_TIERS` (iwn/nwn z8, rwn z10, lwn z12). Each is authoritative for its own layer; this glossary defines the tiers, not the zooms — but the activities and server docs should reconcile the two accounts.
5. **`PROMOTE_PATHS`** — a comment in [`scripts/build.mjs`](scripts/build.mjs) references this section name, but no such section exists in the code; believed to be a legacy name.
6. **Handoff, two senses** — used for both the z12–14 paths continuity and a road-casing zoom-transition flash in the build script's comments.

---

**→ [Next: Build Script](1.build.md)**
